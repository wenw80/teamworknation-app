"use client";

import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function ResetPasswordPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Reset Password</CardTitle>
      </CardHeader>
      <CardContent>
        {/* TODO: Phase 5 — New password form */}
        <p className="text-sm text-muted-foreground">Password reset coming in Phase 5.</p>
      </CardContent>
    </Card>
  );
}

"use client";

import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function ForgotPasswordPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Forgot Password</CardTitle>
      </CardHeader>
      <CardContent>
        {/* TODO: Phase 5 — Password reset request form */}
        <p className="text-sm text-muted-foreground">Password reset coming in Phase 5.</p>
      </CardContent>
    </Card>
  );
}

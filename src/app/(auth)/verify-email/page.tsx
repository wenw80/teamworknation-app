"use client";

import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function VerifyEmailPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Verify Your Email</CardTitle>
      </CardHeader>
      <CardContent>
        {/* TODO: Phase 5 — Email verification handling */}
        <p className="text-sm text-muted-foreground">Email verification coming in Phase 5.</p>
      </CardContent>
    </Card>
  );
}
